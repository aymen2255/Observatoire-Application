{% extends 'BusinessOrganisationBundle::layout.html.twig' %}
{% block body %}
<h1>Cfamily creation</h1>

<form action="{{ path('cfamily_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cfamily') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}